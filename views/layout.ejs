<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SalePoint - Inventory Management</title>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Roboto:wght@300;400;500&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
</head>

<body>
<% if (path !== "/auth/sign-in" && path !== "/auth/sign-up") { %>
    <nav>
        <div class="container">
            <span><a href="/" class="nav-logo">SalePoint<sup>CM</sup></a></span>
            <button class="nav-toggle" id="navToggle">☰</button>
            <div class="nav-links" id="navLinks">
                <a <%= path=="/products" ? "class=active" : "" %> href="/products">Products</a>
                <a <%= path=="/customers" ? "class=active" : "" %> href="/customers">Customers</a>
                <a <%= path=="/vendors" ? "class=active" : "" %> href="/vendors">Vendors</a>
                <% if (user) { %>
                <a <%= path=="/auth/logout" ? "class=active" : "" %> href="/auth/logout" onclick="return confirm('Are you sure you want to logout?')">Logout</a>
                <% } else { %>
                <a <%= path=="/auth/sign-up" ? "class=active" : "" %> href="/auth/sign-up">Sign Up</a>
                <a <%= path=="/auth/sign-in" ? "class=active" : "" %> href="/auth/sign-in">Sign In</a>
                <% } %>
            </div>
        </div>
    </nav>
    <hr>
    <% } %>
    <!-- Main Content Area -->
    <div class="container py-4 overflow-x-hidden">
        <%- body %>
    </div>
    <% if (path !== "/auth/sign-in" && path !== "/auth/sign-up") { %>
    <script>
        // Mobile navigation toggle
        document.getElementById('navToggle').addEventListener('click', function () {
            document.getElementById('navLinks').classList.toggle('active');
        });
    </script>
    <% } %>
</body>

</html>